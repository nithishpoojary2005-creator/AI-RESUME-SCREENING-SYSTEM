{
  "name": "ai-resume-screening-suite",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "backend": "uvicorn app.main:app --app-dir backend --reload"
  }
}
